version: "3.9"

services:
  db:
    container_name: db
    image: postgres:12
    ports: 
      - 5432:5432
    environment:
      - POSTGRES_USER=marioto
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=defaultdb